function repeatStr (n, s) {
    let finalWord = "";
    for (let i = 0; i < n; i++) {
        finalWord += s;        
    }
    return finalWord;
  }
  
  // or
  
  function repeatStr (n, s) {
    return s.repeat(n);
  }
